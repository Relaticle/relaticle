<?php

declare(strict_types=1);

/*
 |---------------------------------------------------------------------------
 | Regression test stub — generated by manual-testing skill.
 |
 | CHOOSE THE RIGHT KIND OF TEST FIRST:
 |
 |   - Filament/Livewire validation, action, or component-level bug
 |     → Pest Livewire feature test using livewire(...) helper
 |     → Lives at tests/Feature/Filament/... or tests/Feature/Livewire/...
 |     → Use the LIVEWIRE template below.
 |
 |   - Bug only reproducible through real browser (JS, CSS, multi-page flow)
 |     → Pest browser test using $this->browse(...)
 |     → Lives at tests/Browser/...
 |     → Use the BROWSER template below.
 |
 | Default to LIVEWIRE — most Filament bugs reproduce there. Use BROWSER only
 | when the bug genuinely requires a real browser (visual layout, JS execution,
 | drag-and-drop, multi-tab sync).
 |
 | Then either:
 |   (a) Append the it(...) block to an existing test file that already covers
 |       this Resource/feature (search recursively: find tests -iname '*<resource>*' -type f),
 |   (b) Or save the whole file at the most appropriate path.
 |
 | The regression test must run and pass before the skill stages it for commit:
 |   php artisan test --compact --filter="regresses <bug-slug>"
 |---------------------------------------------------------------------------
*/

// === LIVEWIRE TEMPLATE (default) ===
//
// use function Pest\Livewire\livewire;
// use App\Filament\Resources\{{ResourceName}};
//
// mutates({{SourceClass}}::class);
//
// it('regresses {{bug-slug}}: {{one-line summary}}', function () {
//     // Externalize: {{Real-user impact paragraph from the original finding.}}
//
//     $user = App\Models\User::factory()->create();
//     $this->actingAs($user);
//
//     livewire({{ResourceName}}\Pages\Create{{Resource}}::class)
//         ->fillForm([
//             // {{minimum form data to trigger the bug}}
//         ])
//         ->call('create')
//         // Oracle ({{FEW HICCUPPS letter}}): {{what the original bug violated}}
//         ->assertHasFormErrors(['{{field}}' => '{{rule}}']);
// });

// === BROWSER TEMPLATE (use only when JS/CSS/multi-page is essential) ===
//
// mutates({{SourceClass}}::class);
//
// it('regresses {{bug-slug}}: {{one-line summary}}', function () {
//     // Externalize: {{Real-user impact paragraph}}
//
//     // Arrange — smallest seed for repro
//     // {{e.g., $user = User::factory()->create(); $this->actingAs($user);}}
//
//     $this->browse(function ($browser) {
//         // {{Smallest-repro steps from RIMGEA-Isolate.}}
//
//         // Oracle ({{FEW HICCUPPS letter}}): {{what the original bug violated}}
//         $browser->assertSee('{{expected good state}}');
//     });
// });
