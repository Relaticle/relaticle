includes:
    - vendor/larastan/larastan/extension.neon
    - phpstan-baseline.neon

parameters:
    level: 3

    paths:
        - app
        - config
        - bootstrap
        - database/factories
        - routes
