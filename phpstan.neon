includes:
    - vendor/larastan/larastan/extension.neon
    - phpstan-baseline.neon

parameters:
    level: 3

    paths:
        - app
        - app-modules
        - config
        - bootstrap
        - database/factories
        - routes

    configDirectories:
        - config
        - app-modules/Documentation/config
