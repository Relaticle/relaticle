includes:
    - vendor/larastan/larastan/extension.neon
    - phpstan-baseline.neon

parameters:
    level: 2

    paths:
        - app
        - config
        - bootstrap
        - database/factories
        - routes
